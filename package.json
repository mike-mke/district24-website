{
  "name": "district24-website",
  "version": "1.0.0",
  "description": "Choose a self-explaining name for your project.",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/main.jsx --bundle --outfile=js/index.compiled.js --jsx-factory=React.createElement --jsx-fragment=React.Fragment --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@github-mike.com:mike-mke/district24-website.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
