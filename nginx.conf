server {
#     listen      8080;
    listen      80;
    server_name _;
    root        /usr/share/nginx/html;

    # ── main site ─────────────────────────────────────────────
    location / {
        try_files $uri $uri/ /index.html;
    }
}
